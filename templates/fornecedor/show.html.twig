{% extends 'base.html.twig' %}

{% block title %}Fornecedor{% endblock %}

{% block body %}
    <h1>Fornecedor</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ fornecedor.id }}</td>
            </tr>
            <tr>
                <th>Nome</th>
                <td>{{ fornecedor.nome }}</td>
            </tr>
            <tr>
                <th>Cnpj</th>
                <td>{{ fornecedor.cnpj }}</td>
            </tr>
            <tr>
                <th>Descricao</th>
                <td>{{ fornecedor.descricao }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_fornecedor_index') }}">back to list</a>

    <a href="{{ path('app_fornecedor_edit', {'id': fornecedor.id}) }}">edit</a>

    {{ include('fornecedor/_delete_form.html.twig') }}
{% endblock %}
