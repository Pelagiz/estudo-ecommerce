<form method="post" action="{{ path('app_fornecedor_delete', {'id': fornecedor.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fornecedor.id) }}">
    <button class="btn">Delete</button>
</form>
